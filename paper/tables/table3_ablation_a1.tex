% Table III — Ablation A1: prototype query refine (verifies C2).
% Requires: \usepackage{booktabs}
% Provenance: paper/plan/data-collection-checklist.md / experiment-protocol.md §5.A1.
%   x4 ablation, finetuned from the full-functioning checkpoint net_g_250000 for an 80k budget.
%   Metrics parsed from training logs at iter 80000 (val on Set5 + Urban100, Y channel, crop_border=4).
%   entropy_norm = block-averaged normalized prototype-usage entropy on Urban100 (8 TAB blocks).
% IMPORTANT (honest disclosure): all variants share the SAME full-functioning initialization and a SHORT
%   80k finetune budget; the full model is best but the margin is small (~0.04-0.05 dB) and must NOT be
%   over-read as a large isolated gain. The ablation is reported for mechanism + quality (mildly positive),
%   not for a strong absolute-PSNR claim.
\begin{table}[t]
\centering
\caption{Ablation A1 ($\times 4$): prototype query refine. PSNR(dB)/SSIM (Y channel) at iter 80k and
block-averaged normalized prototype-usage entropy on Urban100. Enabling refine gives the better
reconstruction on both benchmarks under the short finetune protocol; it is retained as it improves
(and does not degrade) quality while keeping slot assignment stable.}
\label{tab:abl_a1}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{l cc c}
\toprule
\multirow{2}{*}{Variant} & \multicolumn{2}{c}{Set5} & Urban100 \\
\cmidrule(lr){2-3}
 & PSNR & SSIM & PSNR/SSIM \\
\midrule
w/o refine               & 32.5494 & 0.8995 & 26.8358 / 0.8065 \\
w/\ \ refine (default)   & 32.5878 & 0.9001 & 26.8905 / 0.8086 \\
\bottomrule
\end{tabular}%
}

\vspace{2pt}
{\footnotesize
\textit{Notes.} $\times 4$ ablation finetuned from the full-functioning checkpoint
(\texttt{net\_g\_250000}) for an 80k budget; metrics read at iter 80k. Enabling refine yields the better
PSNR/SSIM on both Set5 and Urban100, though all variants share this initialization and a short budget so
the margin (\,$\sim$0.04--0.05\,dB\,) should not be over-read as a large isolated gain. The design
motivation of dynamic prototypes vs.\ history-center+EMA is supported indirectly by the cited CATANet
results (Table~I), not by this controlled ablation.}
\end{table}
